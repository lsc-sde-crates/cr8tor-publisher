apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-approval-service
  labels:
    xlscsde.nhs.uk/app: {{ .Release.Name }}-approval-service
spec:
  selector:
    xlscsde.nhs.uk/app: {{ .Release.Name }}-approval-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
type: ClusterIP